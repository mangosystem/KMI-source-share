import { motion } from 'framer-motion';
import { useRef } from 'react';

const MarineSustain = () => {
	const scrollRef = useRef();
	return <motion.div className="marine-sustain">푸른 미래를 위한 약속: 해양 지속 가능성</motion.div>;
};

export default MarineSustain;
